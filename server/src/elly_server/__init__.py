"""elly-server: the local backend for Elly (notebook + diary + calendar + habits).

For now this package's only real entry point is the MCP server --
see `elly_server.mcp_server.server`. A REST API for the future PWA will
live alongside it later, reusing the same `elly_server.domain` layer.
"""
