"""Elly's Telegram remote-access bot (separate process, same domain layer).

Run with `uv run elly-telegram`. See PLAN.md section 0.1 and
SECURITY.md for the design rationale (pairing model, offline queueing
caveats).
"""
